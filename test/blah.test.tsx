// import { useFilesAsZip } from '../src';

describe('it', () => {
  it('Correct import', () => {
    // const { files, saveAsZip, urls, setFilesAsZip } = useFilesAsZip();
    return undefined;
  });
});
